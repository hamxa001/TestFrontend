export enum LeaveStatus {
    Pending = 0,
    Success,
    Rejected
}

export enum LeaveType {
    Sick = 0,
    Annual,
    Study,
    Maternal
}

export enum NatureOfLeave {
    Local = 0,
    Travel
}